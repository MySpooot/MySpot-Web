import React, { FC } from 'react';

const NotFound: FC = () => {
    return (
        <div>
            NotFound!
            <br /> Available route path: /login, /home
        </div>
    );
};

export default NotFound;
